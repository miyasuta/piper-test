_schema-version: "2.0.0"
ID: "piper-test"
version: 0.0.1

modules:
  - name: webapp
    type: html5
    path: .
    parameters:
      version: 0.0.1-${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist