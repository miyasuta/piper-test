_schema-version: "2.0.0"
ID: "demo.piper-test"
version: 0.0.1

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "piper-test"
    type: html5
    path: webapp
    parameters:
      name: "piper-test"
      version: 0.0.1-${timestamp}
    build-parameters:
      builder: npm
      build-result: dist